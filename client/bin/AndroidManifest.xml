<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ceri.m2.projet.coeptus"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" >
    </uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" >
    </uses-permission>

    <application
        android:name=".Global"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CommandeSimple"
            android:label="@string/title_activity_commande_simple" >
        </activity>
        <activity
            android:name=".Accelerometer"
            android:label="@string/title_activity_accelerometer" >
        </activity>
        <activity
            android:name=".PathRecord"
            android:label="@string/title_activity_path_record" >
        </activity>
        <activity
            android:name=".Testt"
            android:label="@string/title_activity_testt" >
        </activity>
        <activity
            android:name=".ReplayPath"
            android:label="@string/title_activity_replay_path" >
        </activity>
    </application>

</manifest>
